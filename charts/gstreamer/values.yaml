#

application:
  namespace: smarter
  appName: smarter-gstreamer

image:
  repository: ghcr.io/smarter-project/gstreamer
  # @default -- chart.appVersion
  tag: ""
  pullPolicy: IfNotPresent

configuration:
  video_id: 0
